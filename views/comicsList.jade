extends layout

block content
  h1.
    Comic Book Collection
  ul
    each comic, i in comicsList
      li= comic.bookTitle + " " + comic.seriesTitle
        //Saving the code below to experiment with later
        //a(href="mailto:#{comic.bookTitle}")= comic.bookTitle